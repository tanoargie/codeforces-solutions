{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "preRunCommands": [ "settings set target.input-path ${workspaceFolder}/input.txt" ],
            "program": "${workspaceFolder}/output"
        }
    ]
}
